{
    "getAll": {
        "description": "Gets all entries from 'pacientesconedad'",
        "route": "/pacientesconedad",
        "method": "GET",
        "sql": "SELECT * FROM pacientesconedad",
        "auth_required": false,
        "allowed_roles": [
            "*"
        ]
    }
}